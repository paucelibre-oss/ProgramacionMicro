/*
 * myApp_GPIO.c
 *
 *  Created on: Oct 8, 2025
 *      Author: Paulo
 */
#include "gpio.h"

static void actionPin(GPIO_TypeDef* GPIOx, uint16_t GPIO_Pin);

void gpioInit(void){
	GPIO_Init();
}

void nfcSS(
	_gpioState state
){

	if(state == TURN_ON){
		GPIO_Set(SPI3_CS_GPIO_Port, SPI3_CS_Pin);
		return;
	}

	if(state == TURN_OFF){
		GPIO_Reset(SPI3_CS_GPIO_Port, SPI3_CS_Pin);
		return;
	}
}

static void actionPin(
	GPIO_TypeDef* GPIOx,
	uint16_t GPIO_Pin
){

}

